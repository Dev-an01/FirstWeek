# Embedding Service Requirements
# ==============================
# Minimal dependencies for standalone embedding generation

# Core FastAPI
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Embedding models
sentence-transformers>=2.2.0
torch>=2.0.0

# Database (PostgreSQL only - no Neo4j needed)
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Task queue (for async batch processing)
celery>=5.3.0
redis>=5.0.0

# Utilities
numpy>=1.24.0
psutil>=5.9.0
